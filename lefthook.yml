pre-commit:
  parallel: true
  commands:
    lint:
      glob: "*.ts"
      run: pnpm eslint --fix {staged_files}
      stage_fixed: true
    format:
      run: pnpm prettier --write --ignore-unknown {staged_files}
      stage_fixed: true
